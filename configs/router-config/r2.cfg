enable
configure terminal
hostname R2

! --- 1. External (ISP) Configuration ---
interface GigabitEthernet0/0/0
 description ** Link to ISP Router 2 **
 ip address 198.51.100.2 255.255.255.252
 no shutdown
 ip nat outside

! --- 2. Internal (LAN) Configuration ---
interface GigabitEthernet0/0/1
 description ** Link to SW-2 V99 Access Port **
 ip address 192.168.99.3 255.255.255.0
 no shutdown
 ip nat inside

! --- 3. HSRP Configuration (Standby) ---
standby 1 ip 192.168.99.254  ! Virtual IP (default priority 100)

! --- 4. Default Route to ISP ---
ip route 0.0.0.0 0.0.0.0 198.51.100.1

! --- 5. NAT Configuration ---
! Define the internal networks that will use NAT
access-list 1 permit 192.168.10.0 0.0.0.255
access-list 1 permit 192.168.20.0 0.0.0.255
access-list 1 permit 192.168.99.0 0.0.0.255

! Configure NAT using the outside interface (Overload/PAT)
ip nat inside source list 1 interface GigabitEthernet0/0/0 overload

end
write